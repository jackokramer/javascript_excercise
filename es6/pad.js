let example = 'Dylan';



//console.log(example.padStart(10, 'a'));
//console.log(example.padEnd(10, 'a'));
// NOTE: doesn't work if the string has a space in it

// padd start allows to add values to the start of the string  and to the end of the string depending on long it is


let examples = 'YouTube.com/CodingTutorials360';

console.log(examples.padStart(100));
console.log(examples.padEnd(1));