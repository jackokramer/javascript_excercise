if (false) {
    let example = 5;
}

//console.log(example)

const example = 5; // number string or boolean read only property its different with an array since it'll let you set up a push

console.log(example)

/*
var example;

if (false) {
    example = 5;
}

*/

//hoisting is the important to understand this. JS see's whars in the commented out lines in 8-14
// let uses a blocked scope